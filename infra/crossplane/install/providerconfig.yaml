apiVersion: gcp.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  projectID: cloud-project-493810
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: crossplane-gcp-creds
      key: credentials
